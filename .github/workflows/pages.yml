name: Deploy 본성 스테이지 UI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      server_url:
        description: "Optional external HTTPS 본성 스테이지 server URL to verify and connect"
        required: false
        type: string
        default: ""
      save_verified_server_url:
        description: "Save server_url as BONSUNG_API_BASE_URL after verification"
        required: true
        type: boolean
        default: true

permissions:
  contents: read
  pages: write
  id-token: write
  actions: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup pnpm
        run: |
          corepack enable
          corepack prepare pnpm@11.0.7 --activate

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint

      - name: Verify cleanup boundaries
        run: pnpm verify:stage-cleanup

      - name: Verify 본성 스테이지 release configuration
        if: github.event_name != 'pull_request' && (vars.BONSUNG_API_BASE_URL != '' || inputs.server_url != '') && github.ref == 'refs/heads/main'
        env:
          BONSUNG_API_BASE_URL: ${{ inputs.server_url || vars.BONSUNG_API_BASE_URL }}
          NEXT_PUBLIC_ENABLE_APPS_SCRIPT_TRANSITION: ${{ vars.ENABLE_APPS_SCRIPT_TRANSITION }}
          BONSUNG_RELEASE_ALLOW_TRANSITION_FLAGS: ${{ vars.BONSUNG_RELEASE_ALLOW_TRANSITION_FLAGS }}
        run: pnpm verify:stage-release

      - name: Build Next.js static site
        env:
          NEXT_PUBLIC_BONSUNG_API_BASE_URL: ${{ inputs.server_url || vars.BONSUNG_API_BASE_URL }}
          NEXT_PUBLIC_ENABLE_APPS_SCRIPT_TRANSITION: ${{ vars.ENABLE_APPS_SCRIPT_TRANSITION }}
        run: pnpm build:pages

      - name: Verify 본성 스테이지 server contract
        if: vars.BONSUNG_API_BASE_URL != '' || inputs.server_url != ''
        env:
          BONSUNG_SERVER_VERIFY_BASE_URL: ${{ inputs.server_url || vars.BONSUNG_API_BASE_URL }}
        run: pnpm verify:stage-server

      - name: Save verified 본성 스테이지 server URL
        if: github.event_name == 'workflow_dispatch' && inputs.server_url != '' && inputs.save_verified_server_url
        env:
          GH_TOKEN: ${{ github.token }}
          BONSUNG_API_BASE_URL: ${{ inputs.server_url }}
        run: gh variable set BONSUNG_API_BASE_URL --repo "$GITHUB_REPOSITORY" --body "$BONSUNG_API_BASE_URL"

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    if: github.ref == 'refs/heads/main'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
